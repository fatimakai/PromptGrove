# PromptGrove case study (draft — publish after live verification)

PromptGrove is a Laravel 12 and MySQL prompt library for individuals and lightweight teams. It supports versioned prompts, public discovery, shared collections with Owner/Editor/Viewer permissions, AI-assisted analysis through OpenRouter, Google/GitHub OAuth, custom TOTP/recovery-code two-factor authentication, a token-authenticated API, and provider-isolated test-mode Pro checkout integrations for Stripe, Razorpay, and PayPal. The hosted demo exposes Stripe Checkout only. Razorpay and PayPal remain fully implemented and covered by mocked tests, but are not provider-validated because sandbox-account signup is unavailable from Pakistan.

The project began as a code-snippet manager and was rebuilt around prompt workflows. A prompt has one ownership context: personal, public, or one shared collection. Plan entitlements gate version history and collections; role policies and audited collection mutations protect shared work. The security pass addresses authorization, input validation, rate limits on AI cost, security headers, signed billing callbacks, and replay-resistant webhooks. See `SECURITY.md` and `docs/ARCHITECTURE_FLOWS.md` for the detailed decisions and end-to-end flows.

The planned public demo runs in a Dockerized Laravel container on Render Free with Aiven MySQL, synchronous jobs, OAuth-only signup, public seed prompts, and Stripe test-mode billing. Email delivery is deliberately not advertised as live: there is no verified sending domain in the free-subdomain demo. No real payments are accepted. The operational checklist and known limitations are in `docs/DEPLOYMENT.md`.

**Before publishing:** add the live demo URL, repository link, screenshots, verified OAuth/AI/sandbox-payment test results, test-suite totals, and a short description of the most meaningful implementation tradeoff. Do not claim a provider integration or live deployment was verified until it has been tested against its actual sandbox/account.
